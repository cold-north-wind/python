#include <stdio.h>

void sum_a_b(int a,int b)
{
    sum = a + b;
    return sum
}

void for_exa(*arg)
{
    char *index = *arg;
    while(index != '\n')
    {
        print(*index)
        result = sum_a_b(*index,*index++)
        print(result)
    }
}
